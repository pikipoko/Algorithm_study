# 반복문 & 조건문

## 코드 템플릿
```java
public class Main()
{
    public static void main(String[] args) throws Exception
    {
        // 기본 for
        for(int i = 0; i < n; i++) {}
        
        // 향상된 for (for-each)
        for(int x : arr) {}
        for(String s : list) {}
        for(Map.Entry<String, Integer> e : map.entrySet()) {}
        
        // while
        while (조건) {}
        
        // 삼항 연산자
        int max = (a > b) ? a : b;
    }
}
```